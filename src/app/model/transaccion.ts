export interface TransaccionInterface {
    id_tra ?: number, 
    num_cue ?: string, 
    fec_tra ?: Date,
    nom_usu ?: string
    valor ?: number,
    fec_ini ?: Date,
    fec_fin ?: Date,
    cantidad ?: number,
    total ?: number,
    est_tra ?: boolean
}