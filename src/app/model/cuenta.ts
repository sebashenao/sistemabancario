export interface CuentaInterface {
    num_cue ?: string, 
    pas_cue ?: string, 
    ced_usu ?: string, 
    nom_usu ?: string, 
    id_tip ?: number,
    des_tip ?: string,
    est_cue ?: boolean
}