export interface UsuarioInterface {
    id ?: number, 
    documento ?: string, 
    nombre ?: string,
    usuario ?: string,
    edad ?: number, 
    clave ?: string,
    fecha_creacion ?: Date,
    estado ?: boolean
}